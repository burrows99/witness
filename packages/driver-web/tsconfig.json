{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist",
    "lib": [
      "ES2022",
      "DOM"
    ]
  },
  "include": [
    "src/**/*.ts"
  ],
  "references": [
    {
      "path": "../core"
    },
    {
      "path": "../driver-api"
    },
    {
      "path": "../recorders"
    }
  ],
  "//": "This package contains code that runs inside the browser (the recording overlay), so it needs DOM types as well as node."
}
