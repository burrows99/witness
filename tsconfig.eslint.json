{
  "//": "Type-aware linting needs every file in one program, and tsconfig.json deliberately includes only src/. This one covers the whole repo and emits nothing.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "declaration": false,
    "declarationMap": false,
    "types": [
      "node"
    ],
    "lib": [
      "ES2022",
      "DOM"
    ],
    "rootDir": "."
  },
  "include": [
    "src/**/*.ts",
    "test/**/*.ts",
    "*.ts"
  ]
}
