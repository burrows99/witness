{
  "name": "@burrows99/witness",
  "version": "0.1.47",
  "description": "Drive a running system from one config file, and come back with evidence rather than a pass/fail.",
  "keywords": [
    "e2e",
    "testing",
    "evidence",
    "playwright",
    "docker",
    "harness",
    "config-driven",
    "screenshots",
    "video",
    "agents"
  ],
  "license": "MIT",
  "author": "burrows99",
  "type": "module",
  "engines": {
    "node": ">=22.6"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/burrows99/witness.git"
  },
  "homepage": "https://github.com/burrows99/witness#readme",
  "bugs": {
    "url": "https://github.com/burrows99/witness/issues"
  },
  "bin": {
    "witness": "dist/bin/cli.js"
  },
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    },
    "./package.json": "./package.json",
    "./skill": {
      "types": "./dist/src/skill/skill.d.ts",
      "default": "./dist/src/skill/skill.js"
    }
  },
  "files": [
    "dist",
    "src",
    "!src/**/*.test.ts",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "prepare": "npm run build --if-present",
    "test": "node --test --disable-warning=MODULE_TYPELESS_PACKAGE_JSON \"src/**/*.test.ts\" \"bin/**/*.test.ts\"",
    "typecheck": "tsc -p tsconfig.json",
    "witness": "node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON bin/cli.ts",
    "prepublishOnly": "npm run build",
    "lint": "eslint .",
    "deadcode": "knip",
    "package": "npm pack --dry-run --silent >/dev/null && publint --strict",
    "check": "npm run typecheck && npm run lint && npm run deadcode && npm test",
    "banner": "playwright screenshot --viewport-size=1280,640 --browser=chromium docs/banner/banner.html docs/banner.png"
  },
  "peerDependencies": {
    "@playwright/test": ">=1.40"
  },
  "peerDependenciesMeta": {
    "@playwright/test": {
      "optional": true
    }
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.50.0",
    "@types/node": "^22.10.0",
    "eslint": "^10.9.0",
    "knip": "^6.32.2",
    "publint": "^0.3.24",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.67.0"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com",
    "access": "public"
  }
}
